function add(num1: number, num2: number, showRes: boolean) {
  return num1 + num2;

  if (showRes) {
    console.log(result);
  }
}

const n1 = 5,
  n2 = 2.8,
  printResult = true;

const result = add(n1, n2, printResult);
